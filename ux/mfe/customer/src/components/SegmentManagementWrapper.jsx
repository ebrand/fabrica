// Note: No CSS import here - the shell provides Tailwind
import SegmentManagement from '../pages/SegmentManagement';

export default function SegmentManagementWrapper(props) {
  return <SegmentManagement {...props} />;
}
